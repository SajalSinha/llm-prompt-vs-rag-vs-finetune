You are an expert information extraction system.

Given the following document, extract the requested fields.
If a field is not present, return an empty string.

Return ONLY valid JSON matching the schema.

Document:
{{document}}